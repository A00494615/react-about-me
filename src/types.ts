export interface PersonalInfo {
  name: string;
  profession: string;
  location: string;
  company: string;
  programReason: string;
}

export interface TownInfo {
  name: string;
  description: string[];
}